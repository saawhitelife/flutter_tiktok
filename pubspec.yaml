name: tik_tok_flutter
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  marquee: ^1.7.0
  cupertino_icons: ^1.0.0
  cached_network_image: ^2.5.0

dev_dependencies:
  effective_dart: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true